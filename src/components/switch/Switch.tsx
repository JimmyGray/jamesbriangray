import styled from '@emotion/styled';
import React from 'react';

const Container = styled.div({
  position: 'fixed',
  bottom: 0,
  left: 0
});

export const Switch = () => (
    <Container>
      Switch
    </Container>
);